# Slack Bot User OAuth Token (필수)
# https://api.slack.com/apps → 만든 앱 → "OAuth & Permissions" → "Bot User OAuth Token" 복사
SLACK_BOT_TOKEN=xoxb-여기에-토큰-붙여넣기

# 서버 포트 (선택, 기본값 3000)
PORT=3000

# 타임존 (선택, 기본값 Asia/Seoul)
TZ=Asia/Seoul
